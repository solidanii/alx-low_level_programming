#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * _puts - function to print
 * Return: always 0
 */
int main(void)
{
	puts("\"programming is like building a multilingual puzzle");
	return (0);
}
