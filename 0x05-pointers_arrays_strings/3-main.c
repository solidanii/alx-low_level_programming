#include "main.h"
/**
 * main - check the function
 * Return: always 0
 */

int main(void)
{
	char *str;

	str = " i do not fear computers. ifear the lack of them -Isaac asimov";
	_puts(str);
	return (0);
}
