#include "main.h"
/**
 * main - check function
 * Return: always 0
 */

int main(void)
{
	char *str;

	str = "i do not fear computers. i fear the lack of them - isaac asimov";
	print_rev(str);
	return (0);
}
